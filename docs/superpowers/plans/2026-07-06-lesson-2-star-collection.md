# 第 2 课：星星收集和分数 UI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在现有玩家移动场景里加入一颗可收集星星和一个分数 Label，玩家碰到星星后分数从 0 变成 1，星星隐藏。

**Architecture:** 第 2 课只引入两个新组件：`StarView` 负责画出不依赖图片资源的黄色收集物，`GameManager` 负责引用玩家、星星和分数 Label，并在每帧检测玩家是否接近星星。先做一次性收集，不做随机生成和多颗星星，把注意力放在节点引用、UI Label 和简单碰撞判断上。

**Tech Stack:** Cocos Creator 3.8.8、TypeScript、Cocos Node 引用、Cocos Label、Cocos Graphics、简单距离检测。

---

## 当前状态说明

进入第 2 课前，`assets/scenes/Main.scene` 里已有一个未提交的相机 `orthoHeight` 数值变化。这是 Cocos 编辑器保存或预览后带出的场景数值变化，不是第 2 课的核心逻辑。

第 2 课也会修改 `Main.scene`。执行时允许这个相机数值随场景提交一起保留，但提交说明仍以第 2 课新增的星星、分数和节点引用为主。

## 文件结构

- Create: `assets/scripts/StarView.ts`
  - 星星显示组件，用 `Graphics` 画一个黄色圆形收集物。
- Create: `assets/scripts/GameManager.ts`
  - 游戏管理组件，负责分数、玩家和星星的距离检测、收集后更新 UI。
- Modify: `assets/scenes/Main.scene`
  - 添加 `ScoreLabel`、`Star`、`GameManager` 节点，并绑定组件引用。
- Create: `docs/lessons/lesson-2-star-collection.md`
  - 第 2 课中文复盘。
- Generated by Cocos: `assets/scripts/StarView.ts.meta`
- Generated by Cocos: `assets/scripts/GameManager.ts.meta`

## Task 1: 创建星星和游戏管理脚本

**Files:**
- Create: `assets/scripts/StarView.ts`
- Create: `assets/scripts/GameManager.ts`
- Generated by Cocos: `assets/scripts/StarView.ts.meta`
- Generated by Cocos: `assets/scripts/GameManager.ts.meta`

- [ ] **Step 1: 创建 `StarView.ts`**

Create `assets/scripts/StarView.ts` with this content:

```ts
import { _decorator, Color, Component, Graphics, UITransform } from 'cc';

const { ccclass, property } = _decorator;

@ccclass('StarView')
export class StarView extends Component {
    @property
    public radius = 28;

    @property
    public fillColor = new Color(255, 220, 80, 255);

    start() {
        this.draw();
    }

    private draw() {
        let transform = this.getComponent(UITransform);
        if (!transform) {
            transform = this.addComponent(UITransform);
        }

        let graphics = this.getComponent(Graphics);
        if (!graphics) {
            graphics = this.addComponent(Graphics);
        }

        transform.setContentSize(this.radius * 2, this.radius * 2);

        graphics.clear();
        graphics.fillColor = this.fillColor;
        graphics.circle(0, 0, this.radius);
        graphics.fill();
    }
}
```

- [ ] **Step 2: 创建 `GameManager.ts`**

Create `assets/scripts/GameManager.ts` with this content:

```ts
import { _decorator, Component, Label, Node, Vec3 } from 'cc';

const { ccclass, property } = _decorator;

@ccclass('GameManager')
export class GameManager extends Component {
    @property(Node)
    public playerNode: Node | null = null;

    @property(Node)
    public starNode: Node | null = null;

    @property(Label)
    public scoreLabel: Label | null = null;

    @property
    public collectDistance = 58;

    private _score = 0;
    private readonly _playerWorldPosition = new Vec3();
    private readonly _starWorldPosition = new Vec3();

    start() {
        this.updateScoreLabel();
    }

    update() {
        if (!this.playerNode || !this.starNode || !this.starNode.active) {
            return;
        }

        this.playerNode.getWorldPosition(this._playerWorldPosition);
        this.starNode.getWorldPosition(this._starWorldPosition);

        const offsetX = this._playerWorldPosition.x - this._starWorldPosition.x;
        const offsetY = this._playerWorldPosition.y - this._starWorldPosition.y;
        const distanceSquared = offsetX * offsetX + offsetY * offsetY;
        const collectDistanceSquared = this.collectDistance * this.collectDistance;

        if (distanceSquared <= collectDistanceSquared) {
            this.collectStar();
        }
    }

    private collectStar() {
        this._score += 1;

        if (this.starNode) {
            this.starNode.active = false;
        }

        this.updateScoreLabel();
    }

    private updateScoreLabel() {
        if (!this.scoreLabel) {
            return;
        }

        this.scoreLabel.string = `Score: ${this._score}`;
    }
}
```

- [ ] **Step 3: 等待 Cocos 导入脚本**

回到 Cocos Creator，等待资源面板刷新。确认 Assets 面板里出现：

```text
assets/scripts/StarView
assets/scripts/GameManager
```

- [ ] **Step 4: 确认脚本元数据生成**

Run:

```powershell
Get-ChildItem -Force assets\scripts
```

Expected includes:

```text
GameManager.ts
GameManager.ts.meta
StarView.ts
StarView.ts.meta
```

- [ ] **Step 5: Commit**

```powershell
git add assets/scripts
git commit -m "feat: add star collection scripts"
```

## Task 2: 在场景中添加星星、分数和 GameManager

**Files:**
- Modify: `assets/scenes/Main.scene`

- [ ] **Step 1: 创建分数 Label**

在 Cocos Creator 的 Hierarchy 面板中：

```text
Canvas -> 右键 -> Create -> 2D Object -> Label
```

把新节点命名为：

```text
ScoreLabel
```

选中 `ScoreLabel`，在 Inspector 中设置：

```text
Position: x = -520, y = 300
Label String: Score: 0
Font Size: 32
Color: 白色
```

- [ ] **Step 2: 创建 Star 节点**

在 Hierarchy 面板中：

```text
Canvas -> 右键 -> Create -> Create Empty Node
```

把新节点命名为：

```text
Star
```

选中 `Star`，在 Inspector 中设置：

```text
Position: x = 220, y = 0
Scale: x = 1, y = 1
```

然后添加组件：

```text
Add Component -> Custom Script -> StarView
```

保持属性：

```text
Radius: 28
Fill Color: 默认黄色即可
```

- [ ] **Step 3: 创建 GameManager 节点**

在 Hierarchy 面板中：

```text
Canvas -> 右键 -> Create -> Create Empty Node
```

把新节点命名为：

```text
GameManager
```

选中 `GameManager`，添加组件：

```text
Add Component -> Custom Script -> GameManager
```

在 `GameManager` 组件上绑定引用：

```text
Player Node: 拖拽 Hierarchy 里的 Player
Star Node: 拖拽 Hierarchy 里的 Star
Score Label: 拖拽 Hierarchy 里的 ScoreLabel
Collect Distance: 58
```

- [ ] **Step 4: 保存场景**

在 Cocos Creator 中执行：

```text
Ctrl + S
```

- [ ] **Step 5: Commit**

```powershell
git add assets/scenes/Main.scene
git commit -m "feat: add star and score to main scene"
```

## Task 3: 手动运行验证

**Files:**
- No file changes expected.

- [ ] **Step 1: 运行场景**

在 Cocos Creator 顶部点击预览运行按钮。

- [ ] **Step 2: 验证初始状态**

Expected:

```text
屏幕中能看到蓝色 Player
屏幕右侧能看到黄色 Star
屏幕左上角能看到 Score: 0
Console 没有脚本报错
```

- [ ] **Step 3: 验证拾取行为**

Expected:

```text
用 WASD 或方向键移动 Player 靠近 Star
Player 接近 Star 后，Star 隐藏
Score: 0 变成 Score: 1
继续移动时，分数保持 Score: 1
Console 没有脚本报错
```

## Task 4: 记录第 2 课复盘

**Files:**
- Create: `docs/lessons/lesson-2-star-collection.md`

- [ ] **Step 1: 创建复盘文档**

Create `docs/lessons/lesson-2-star-collection.md` with this content:

```md
# 第 2 课：星星收集和分数 UI

## 本课目标

在已有玩家移动基础上，加入一颗星星和一个分数 Label。玩家碰到星星后，星星隐藏，分数从 0 变成 1。

## 今天真正掌握的东西

- `Label` 是 Cocos 里显示文字的常用组件。
- 脚本可以通过 `@property(Node)` 暴露节点引用，然后在 Inspector 里拖拽绑定。
- `GameManager` 适合管理“跨多个对象的规则”，例如玩家碰到星星后更新分数。
- 简单距离检测可以先不用物理系统，用两个节点的世界坐标计算。
- 不要一开始就上复杂碰撞系统；先理解数据流和对象关系更重要。

## 本课的对象关系

```text
GameManager
├─ playerNode -> Player
├─ starNode -> Star
└─ scoreLabel -> ScoreLabel
```

这说明 `GameManager` 自己不显示任何东西，它只是拿到场景里的关键节点，然后协调它们。

## 可以自己试的小改动

- 把 `Collect Distance` 改成 `30` 或 `100`，观察收集距离变化。
- 把 `Star` 的位置改远一点，确认收集逻辑跟节点位置有关。
- 把 `ScoreLabel` 的文字颜色或位置改掉，确认 UI 和游戏规则是分开的。
```

- [ ] **Step 2: Commit**

```powershell
git add docs/lessons/lesson-2-star-collection.md
git commit -m "docs: add lesson 2 recap"
```

## Task 5: 同步第 2 课进度

**Files:**
- No file changes expected.

- [ ] **Step 1: 查看提交历史**

Run:

```powershell
git log --oneline -8
```

Expected includes:

```text
docs: add lesson 2 recap
feat: add star and score to main scene
feat: add star collection scripts
docs: add lesson 1 recap
```

- [ ] **Step 2: 确认工作区干净**

Run:

```powershell
git status -sb
```

Expected:

```text
## main...origin/main [ahead 3]
```

- [ ] **Step 3: 推送到 GitHub**

Run:

```powershell
git push
```

Expected:

```text
main -> main
```

## 自检

- 设计文档中的第 2 课目标由 Task 1 到 Task 4 覆盖。
- 本计划只实现一颗星星、一次收集和分数 UI，不加入随机生成、多颗星星、危险物或失败条件。
- 需要你阅读的内容全部使用中文。
- 没有使用占位描述或未定实现说明。
