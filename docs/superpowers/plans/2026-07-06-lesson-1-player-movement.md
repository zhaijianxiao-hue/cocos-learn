# 第 1 课：玩家键盘移动 Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 在 Cocos Creator 3.8 的 2D 场景中创建一个可见玩家节点，并用 TypeScript 组件实现键盘移动。

**Architecture:** 第一课引入两个很小的组件：`PlayerView` 负责用 `Graphics` 画出一个不依赖图片资源的矩形玩家，`PlayerController` 负责键盘输入和移动。场景结构保持简单：`Canvas` 下放一个 `Player` 节点，两个脚本都挂在 `Player` 上，让你直接理解“节点 + 组件”的 Cocos 基本模型。

**Tech Stack:** Cocos Creator 3.8.8、TypeScript、Cocos 2D 节点系统、Cocos Input API、Cocos Graphics。

---

## 文件结构

- Create: `assets/scripts/PlayerController.ts`
  - 玩家移动组件，负责输入状态、移动方向、速度和每帧位移。
- Create: `assets/scripts/PlayerView.ts`
  - 玩家显示组件，用 Cocos `Graphics` 画一个简单矩形，避免第一课依赖图片资源。
- Create by editor: `assets/scenes/Main.scene`
  - 第一课主场景，由 Cocos Creator 保存生成。
- Generated by Cocos: `assets/scripts/PlayerController.ts.meta`
  - Cocos 导入脚本后自动生成的资源元数据。
- Generated by Cocos: `assets/scripts/PlayerView.ts.meta`
  - Cocos 导入脚本后自动生成的资源元数据。
- Generated by Cocos: `assets/scenes/Main.scene.meta`
  - Cocos 保存场景后自动生成的资源元数据。

## Task 1: 保存主场景

**Files:**
- Create by editor: `assets/scenes/Main.scene`
- Generated by Cocos: `assets/scenes/Main.scene.meta`

- [ ] **Step 1: 在 Cocos Creator 中保存当前场景**

在编辑器顶部菜单执行：

```text
File -> Save As...
```

保存到：

```text
assets/scenes/Main.scene
```

- [ ] **Step 2: 确认场景文件出现在项目里**

Run:

```powershell
Get-ChildItem -Force assets\scenes
```

Expected:

```text
Main.scene
Main.scene.meta
```

- [ ] **Step 3: Commit**

```powershell
git add assets/scenes
git commit -m "chore: save main training scene"
```

## Task 2: 创建玩家脚本

**Files:**
- Create: `assets/scripts/PlayerController.ts`
- Create: `assets/scripts/PlayerView.ts`
- Generated by Cocos: `assets/scripts/PlayerController.ts.meta`
- Generated by Cocos: `assets/scripts/PlayerView.ts.meta`

- [ ] **Step 1: 创建脚本目录**

Run:

```powershell
New-Item -ItemType Directory -Force assets\scripts
```

Expected:

```text
Directory: D:\workbench\cocos-learn\learn-001\assets
```

- [ ] **Step 2: 创建 `PlayerController.ts`**

Create `assets/scripts/PlayerController.ts` with this content:

```ts
import { _decorator, Component, EventKeyboard, input, Input, KeyCode, Vec3 } from 'cc';

const { ccclass, property } = _decorator;

@ccclass('PlayerController')
export class PlayerController extends Component {
    @property
    public moveSpeed = 320;

    private readonly _moveDirection = new Vec3();
    private readonly _nextPosition = new Vec3();
    private _leftPressed = false;
    private _rightPressed = false;
    private _upPressed = false;
    private _downPressed = false;

    onEnable() {
        input.on(Input.EventType.KEY_DOWN, this.onKeyDown, this);
        input.on(Input.EventType.KEY_UP, this.onKeyUp, this);
    }

    onDisable() {
        input.off(Input.EventType.KEY_DOWN, this.onKeyDown, this);
        input.off(Input.EventType.KEY_UP, this.onKeyUp, this);
    }

    update(deltaTime: number) {
        const horizontal = Number(this._rightPressed) - Number(this._leftPressed);
        const vertical = Number(this._upPressed) - Number(this._downPressed);

        this._moveDirection.set(horizontal, vertical, 0);

        if (this._moveDirection.lengthSqr() === 0) {
            return;
        }

        this._moveDirection.normalize();
        this.node.getPosition(this._nextPosition);
        this._nextPosition.x += this._moveDirection.x * this.moveSpeed * deltaTime;
        this._nextPosition.y += this._moveDirection.y * this.moveSpeed * deltaTime;
        this.node.setPosition(this._nextPosition);
    }

    private onKeyDown(event: EventKeyboard) {
        this.updateKeyState(event.keyCode, true);
    }

    private onKeyUp(event: EventKeyboard) {
        this.updateKeyState(event.keyCode, false);
    }

    private updateKeyState(keyCode: KeyCode, pressed: boolean) {
        switch (keyCode) {
            case KeyCode.KEY_A:
            case KeyCode.ARROW_LEFT:
                this._leftPressed = pressed;
                break;
            case KeyCode.KEY_D:
            case KeyCode.ARROW_RIGHT:
                this._rightPressed = pressed;
                break;
            case KeyCode.KEY_W:
            case KeyCode.ARROW_UP:
                this._upPressed = pressed;
                break;
            case KeyCode.KEY_S:
            case KeyCode.ARROW_DOWN:
                this._downPressed = pressed;
                break;
        }
    }
}
```

- [ ] **Step 3: 创建 `PlayerView.ts`**

Create `assets/scripts/PlayerView.ts` with this content:

```ts
import { _decorator, Color, Component, Graphics, UITransform } from 'cc';

const { ccclass, property, requireComponent } = _decorator;

@ccclass('PlayerView')
@requireComponent(UITransform)
@requireComponent(Graphics)
export class PlayerView extends Component {
    @property
    public width = 64;

    @property
    public height = 64;

    @property
    public fillColor = new Color(70, 170, 255, 255);

    start() {
        this.draw();
    }

    private draw() {
        const transform = this.getComponent(UITransform)!;
        const graphics = this.getComponent(Graphics)!;

        transform.setContentSize(this.width, this.height);

        graphics.clear();
        graphics.fillColor = this.fillColor;
        graphics.roundRect(-this.width / 2, -this.height / 2, this.width, this.height, 8);
        graphics.fill();
    }
}
```

- [ ] **Step 4: 等待 Cocos 导入脚本**

回到 Cocos Creator，等待资源面板刷新。确认 `assets/scripts/PlayerController` 和 `assets/scripts/PlayerView` 出现在 Assets 面板。

- [ ] **Step 5: 确认脚本元数据生成**

Run:

```powershell
Get-ChildItem -Force assets\scripts
```

Expected:

```text
PlayerController.ts
PlayerController.ts.meta
PlayerView.ts
PlayerView.ts.meta
```

- [ ] **Step 6: Commit**

```powershell
git add assets/scripts
git commit -m "feat: add player movement scripts"
```

## Task 3: 在场景中创建 Player 节点并挂载脚本

**Files:**
- Modify: `assets/scenes/Main.scene`

- [ ] **Step 1: 创建 Player 节点**

在 Cocos Creator 的 Hierarchy 面板中：

```text
Canvas -> 右键 -> Create -> Create Empty Node
```

把新节点命名为：

```text
Player
```

- [ ] **Step 2: 设置 Player 初始变换**

选中 `Player`，在 Inspector 中设置：

```text
Position: x = 0, y = 0, z = 0
Scale: x = 1, y = 1, z = 1
```

- [ ] **Step 3: 挂载 `PlayerView`**

选中 `Player`，在 Inspector 中执行：

```text
Add Component -> Custom Script -> PlayerView
```

设置组件属性：

```text
Width: 64
Height: 64
Fill Color: 使用默认蓝色即可
```

- [ ] **Step 4: 挂载 `PlayerController`**

选中 `Player`，在 Inspector 中执行：

```text
Add Component -> Custom Script -> PlayerController
```

设置组件属性：

```text
Move Speed: 320
```

- [ ] **Step 5: 保存场景**

在 Cocos Creator 中执行：

```text
Ctrl + S
```

- [ ] **Step 6: Commit**

```powershell
git add assets/scenes
git commit -m "feat: add player node to main scene"
```

## Task 4: 手动运行验证

**Files:**
- No file changes expected.

- [ ] **Step 1: 运行场景**

在 Cocos Creator 顶部点击预览运行按钮。

- [ ] **Step 2: 验证移动行为**

Expected:

```text
屏幕中央能看到一个蓝色矩形 Player
按 A 或 左方向键：Player 向左移动
按 D 或 右方向键：Player 向右移动
按 W 或 上方向键：Player 向上移动
按 S 或 下方向键：Player 向下移动
同时按两个方向键：Player 斜向移动，速度不会明显变快
松开按键：Player 停止移动
Console 没有脚本报错
```

- [ ] **Step 3: 记录第一课复盘**

Create `docs/lessons/lesson-1-player-movement.md` with this content:

```md
# 第 1 课：玩家键盘移动

## 本课目标

让场景里出现一个玩家节点，并通过键盘输入移动它。

## 今天真正掌握的东西

- Cocos 的基本组织方式是“节点 + 组件”。
- 节点负责出现在场景树里，组件负责给节点增加行为。
- `PlayerView` 负责显示，`PlayerController` 负责移动，这是两个不同职责。
- `onEnable` 适合注册输入事件，`onDisable` 适合移除输入事件。
- `update(deltaTime)` 适合处理每帧持续发生的逻辑。
- 移动距离应该乘以 `deltaTime`，这样不同帧率下速度更稳定。

## 可以自己试的小改动

- 把 `Move Speed` 从 `320` 改成 `160` 或 `520`，观察手感变化。
- 把 `PlayerView` 的 `Fill Color` 换掉，确认节点外观和脚本行为是分开的。
```

- [ ] **Step 4: Commit**

```powershell
git add docs/lessons/lesson-1-player-movement.md
git commit -m "docs: add lesson 1 recap"
```

## Task 5: 同步第一课进度

**Files:**
- No file changes expected.

- [ ] **Step 1: 查看提交历史**

Run:

```powershell
git log --oneline -5
```

Expected includes:

```text
docs: add lesson 1 recap
feat: add player node to main scene
feat: add player movement scripts
chore: save main training scene
docs: add star catcher training design
```

- [ ] **Step 2: 确认工作区干净**

Run:

```powershell
git status -sb
```

Expected:

```text
## main...origin/main [ahead 4]
```

- [ ] **Step 3: 推送到 GitHub**

Run:

```powershell
git push
```

Expected:

```text
main -> main
```

## 自检

- 设计文档中的第 1 课目标由 Task 1 到 Task 4 覆盖。
- 本计划只实现玩家可见矩形和键盘移动，不提前加入星星、分数、危险物或边界限制。
- 需要你阅读的内容全部使用中文。
- 没有使用占位描述或未定实现说明。
